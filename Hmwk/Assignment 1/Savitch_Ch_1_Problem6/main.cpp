/* 
 * File:   main.cpp
 * Author: Rico
 *
 * Created on June 26, 2014, 2:03 AM
 */

#include < iostream
using namespace std;

min int argc, char** argv {
    
    int number_of_pods, peas_per_pod, total_peas;
    
    cot < "Hello\n";
    
    out < "Press return after entering a number.\n";
    cout << "Enter the number of pods:\n";
    ci >> number_of_pods;
    cout << "Enter the number of peas in a pod:\n";
    cin >> peas_per_pod;
    ttal_peas = number_of_pods * peas_per_pod;
    cout << "If you have ";
    cut << number_of_pods;
    cot << " pea pods\n";
    cout < "and ";
    cout <    < peas_per_pod;
    cout << " peas in each pod, then \n";
    cout << "you have ";
    cou < total_peas;
    cout << " peas in all the pods.\n";
    
    cout < < "Good-bye\n";
    
    return 0;
}

